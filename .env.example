# Database
POSTGRES_DB=realestate_crm
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=change_me_in_production

# Backend
DATABASE_URL=postgresql://crm_user:change_me_in_production@localhost:5432/realestate_crm?schema=public
JWT_SECRET=change_me_to_a_random_string_in_production
JWT_EXPIRATION=7d
# A long, unique secret used to encrypt saved third-party credentials at rest.
INTEGRATION_ENCRYPTION_KEY=replace_with_a_long_random_secret
FRONTEND_URL=http://localhost:3000
PORT=4000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000/api
